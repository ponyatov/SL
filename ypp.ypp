%{
#include "hpp.hpp"
%}

%defines %union { Sym* o; }            /* we work only with Symbolic objects */

                          /* but can process some literal types individually */
%token <o> SYM					

%%											  /* it's a smell of fried FORTH */
REPL :
	 | REPL SYM { Stack->push($2);
	 			  if (FIND()) EXECUTE();
	 			  else yyerror(Stack->pop()->value); }
%%
